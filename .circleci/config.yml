jobs:
  install_php:
    docker:
      - image: 'cimg/base:edge'
    steps:
      - checkout
      - php/install-php:
          version: '7.0'
orbs:
  php: circleci/php@x.y
version: 2.1
workflows:
  install:
    jobs:
      - install_php
